machine:
  java:
    version: openjdk8
    
dependencies: 
  override:
    - echo 'Skipping Dependency Check'

test:
  override:
  #  - mvn clean verify
    - wget http://nasdanika.org/commons-net-3.5.jar
    - ant -debug -f ftp-upload.xml -lib commons-net-3.5.jar -Dftp.server=$DEPLOY_FTP_SERVER -Dftp.user=$DEPLOY_FTP_USER -Dftp.password=$DEPLOY_FTP_PASSWORD -Dcircle.artifacts=$CIRCLE_ARTIFACTS
    
