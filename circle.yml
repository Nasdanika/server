machine:
  java:
    version: openjdk8
    
dependencies: 
  override:
    - echo 'Skipping Dependency Check'

test:
  override:
    - mvn clean verify
    
  post:
    - mv org.nasdanika.repository/target/repository $CIRCLE_ARTIFACTS    
